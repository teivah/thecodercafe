version: 2

builds:
- id: kv-client
  main: ./res/kv/client   # tell goreleaser where the go.mod + main.go are
  binary: kv-client
  env: [CGO_ENABLED=0]
  goos: [linux, darwin, windows]
  goarch: [amd64, arm64]

archives:
- name_template: "{{ .ProjectName }}-{{ .Os }}-{{ .Arch }}"
  format_overrides:
  - goos: windows
    format: zip

checksum:
  name_template: "checksums.txt"
